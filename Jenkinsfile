node {
    stage('P0-Test') {
        if (env.BRANCH_NAME == 'master') {
            echo 'Success on master branch'
        } 
        else {
            echo "Branch: $env.BRANCH_NAME"
        }
    }
}